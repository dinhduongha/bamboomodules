load database
     from pgsql://postgres:Pass@@Word123@127.0.0.1:5432/odoo16
     into pgsql://postgres:Pass@@Word123@127.0.0.1:5432/bamboo_core
with disable triggers,
     include drop,
     -- drop indexes,
     downcase identifiers,
     -- schema only,
     -- with data only,
     on error resume next
cast column res_bank.id to uuid drop typemod drop default using int-to-uuid,
     column spreadsheet_dashboard.dashboard_group_id to uuid drop typemod drop default using int-to-uuid,
     column account_analytic_line.so_line to uuid drop typemod drop default using int-to-uuid,
     column account_tax_filiation_rel.child_tax to uuid drop typemod drop default using int-to-uuid,
     column account_tax_filiation_rel.parent_tax to uuid drop typemod drop default using int-to-uuid,
     column stock_putaway_rule.stock_package_type_id to uuid drop typemod drop default using int-to-uuid,
     column calendar_popover_delete_wizard.record to uuid drop typemod drop default using int-to-uuid,
     column crm_lead.recurring_plan to uuid drop typemod drop default using int-to-uuid,
     column crm_lead.title to uuid drop typemod drop default using int-to-uuid,
     column res_partner.title to uuid drop typemod drop default using int-to-uuid,
     column gamification_goal_definition.batch_distinctive_field to uuid drop typemod drop default using int-to-uuid,
     column ir_model_constraint.model to uuid drop typemod drop default using int-to-uuid,
     column ir_model_constraint.module to uuid drop typemod drop default using int-to-uuid,
     column ir_model_relation.model to uuid drop typemod drop default using int-to-uuid,
     column ir_model_relation.module to uuid drop typemod drop default using int-to-uuid,
     column mail_template.ref_ir_act_window to uuid drop typemod drop default using int-to-uuid,
     column pos_config.rounding_method to uuid drop typemod drop default using int-to-uuid,
     column pos_order.account_move to uuid drop typemod drop default using int-to-uuid,
     column pos_order.sale_journal to uuid drop typemod drop default using int-to-uuid,
     column product_template.country_of_origin to uuid drop typemod drop default using int-to-uuid,
     column purchase_order_line.product_uom to uuid drop typemod drop default using int-to-uuid,
     column refunded_invoices.original_account_move to uuid drop typemod drop default using int-to-uuid,
     column refunded_invoices.refund_account_move to uuid drop typemod drop default using int-to-uuid,
     column repair_order.product_uom to uuid drop typemod drop default using int-to-uuid,
     column res_users_deletion.user_id_int to uuid drop typemod drop default using int-to-uuid,
     column sale_order_line.product_uom to uuid drop typemod drop default using int-to-uuid,
     column sale_order.incoterm to uuid drop typemod drop default using int-to-uuid,
     column snailmail_letter.report_template to uuid drop typemod drop default using int-to-uuid,
     column stock_move.product_uom to uuid drop typemod drop default using int-to-uuid,
     column web_editor_converter_test.many2one to uuid drop typemod drop default using int-to-uuid,
     column res_bank.country to uuid drop typemod drop default using int-to-uuid,             
     column res_bank.state to uuid drop typemod drop default using int-to-uuid,
     column mailing_trace.mail_mail_id_int to uuid drop typemod drop default using int-to-uuid,
     column mail_notification.sms_id_int to uuid drop typemod drop default using int-to-uuid,
     column hr_employee.country_of_birth to uuid drop typemod drop default using int-to-uuid,
     column ir_act_server.selection_value to uuid drop typemod drop default using int-to-uuid,
     column pos_config_trust_relation.is_trusted to uuid drop typemod drop default using int-to-uuid,
     column pos_config_trust_relation.is_trusting to uuid drop typemod drop default using int-to-uuid,
     
     -- RE-CHECK
     -- column mail_tracking_value.old_value_integer to uuid drop typemod drop default using int-to-uuid,           
     -- column mail_tracking_value.new_value_integer to uuid drop typemod drop default using int-to-uuid,
     column mail_tracking_value.old_value_integer to bigint,           
     column mail_tracking_value.new_value_integer to bigint,

     column hr_leave_employee_type_report.leave_type to uuid drop typemod drop default using int-to-uuid,
     column hr_departure_reason.reason_code to bigint,
     column hr_employee_public.color to bigint,
     column lunch_topping.topping_category to uuid drop typemod drop default using int-to-uuid,

     -- origin: var char
     column product_tag.color to bigint drop default,
     
     -- column stock_package_type.packaging_length to bigint drop default,
     -- column stock_package_type.height to bigint drop default,
     -- column stock_package_type.width to bigint drop default,
     column stock_package_type.packaging_length to "double precision" drop default,
     column stock_package_type.height to "double precision" drop default,
     column stock_package_type.width to "double precision" drop default,

     --- BEGIN SEQUENCE
     -- Odoo 16
     column  account_fiscal_position_template.sequence to bigint,
     column  account_reconcile_model_line_template.sequence to bigint,
     column  account_reconcile_model_template.sequence to bigint,
     column  account_tax_template.sequence to bigint,
     column  mail_tracking_value.tracking_sequence to bigint,
     column payment_icon.sequence to bigint,
     column note_stage.sequence to bigint,
     column note_note.sequence to bigint,
     column note_note.color to bigint,
     column note_tag.color to bigint,

     column account_analytic_distribution_model.sequence to bigint,
     column account_analytic_plan.sequence to bigint,
     column account_asset_depreciation_line.sequence to bigint,
     column account_bank_statement_line.sequence to bigint,
     column account_financial_report.sequence to bigint,
     column account_fiscal_position.sequence to bigint,
     column account_journal.sequence to bigint,
     column account_journal_group.sequence to bigint,
     column account_merge_wizard_line.sequence to bigint,
     column account_move.sequence_number to bigint,
     column account_move.secure_sequence_number to bigint,
     column account_move_line.sequence to bigint,
     column account_payment_method_line.sequence to bigint,
     column account_payment_term.sequence to bigint,
     column account_reconcile_model.sequence to bigint,
     column account_reconcile_model_line.sequence to bigint,
     column account_report.sequence to bigint,
     column account_report_column.sequence to bigint,
     column account_report_line.sequence to bigint,
     column account_tax.sequence to bigint,
     column account_tax_group.sequence to bigint,
     column account_tax_repartition_line.sequence to bigint,
     column barcode_rule.sequence to bigint,
     column chatbot_script_answer.sequence to bigint,
     column chatbot_script_step.sequence to bigint,
     column crm_iap_lead_industry.sequence to bigint,
     column crm_recurring_plan.sequence to bigint,
     column crm_stage.sequence to bigint,
     column crm_team.sequence to bigint,
     column delivery_carrier.sequence to bigint,
     column delivery_price_rule.sequence to bigint,
     column digest_tip.sequence to bigint,
     column event_event_ticket.sequence to bigint,
     column event_mail.sequence to bigint,
     column event_question.sequence to bigint,
     column event_question_answer.sequence to bigint,
     column event_stage.sequence to bigint,
     column event_tag.sequence to bigint,
     column event_tag.category_sequence to bigint,
     column event_tag_category.sequence to bigint,
     column event_type.sequence to bigint,
     column event_type_ticket.sequence to bigint,
     column fleet_vehicle_model_category.sequence to bigint,
     column fleet_vehicle_state.sequence to bigint,
     column gamification_challenge_line.sequence to bigint,
     column hr_applicant_refuse_reason.sequence to bigint,
     column hr_contract_type.sequence to bigint,
     column hr_departure_reason.sequence to bigint,
     column hr_job.sequence to bigint,
     column hr_leave_accrual_level.sequence to bigint,
     column hr_leave_type.sequence to bigint,
     column hr_recruitment_degree.sequence to bigint,
     column hr_recruitment_stage.sequence to bigint,
     column hr_resume_line_type.sequence to bigint,
     column hr_skill.sequence to bigint,
     column im_livechat_channel_rule.sequence to bigint,
     column ir_act_server.sequence to bigint,
     column ir_act_window_view.sequence to bigint,
     column ir_actions_todo.sequence to bigint,
     column ir_asset.sequence to bigint,
     column ir_embedded_actions.sequence to bigint,
     column ir_mail_server.sequence to bigint,
     column ir_model_fields_selection.sequence to bigint,
     column ir_module_category.sequence to bigint,
     column ir_module_module.sequence to bigint,
     column ir_ui_menu.sequence to bigint,
     column mail_activity_plan_template.sequence to bigint,
     column mail_activity_type.sequence to bigint,
     column mail_alias_domain.sequence to bigint,
     column mail_message_subtype.sequence to bigint,
     column mailing_subscription_optout.sequence to bigint,
     column maintenance_stage.sequence to bigint,
     column mrp_bom.sequence to bigint,
     column mrp_bom_byproduct.sequence to bigint,
     column mrp_bom_line.sequence to bigint,
     column mrp_production.backorder_sequence to bigint,
     column mrp_routing_workcenter.sequence to bigint,
     column mrp_workcenter.sequence to bigint,
     column mrp_workcenter_productivity_loss.sequence to bigint,
     column mrp_workorder.sequence to bigint,
     column onboarding_onboarding.sequence to bigint,
     column onboarding_onboarding_step.sequence to bigint,
     column payment_method.sequence to bigint,
     column payment_provider.sequence to bigint,
     column pos_category.sequence to bigint,
     column pos_note.sequence to bigint,
     column pos_order.sequence_number to bigint,
     column pos_payment_method.sequence to bigint,
     column pos_self_order_custom_link.sequence to bigint,
     column pos_session.sequence_number to bigint,
     column product_attribute.sequence to bigint,
     column product_attribute_value.sequence to bigint,
     column product_combo.sequence to bigint,
     column product_document.sequence to bigint,
     column product_image.sequence to bigint,
     column product_packaging.sequence to bigint,
     column product_pricelist.sequence to bigint,
     column product_public_category.sequence to bigint,
     column product_supplierinfo.sequence to bigint,
     column product_tag.sequence to bigint,
     column product_template.sequence to bigint,
     column product_template.website_sequence to bigint,
     column product_template_attribute_line.sequence to bigint,
     column project_project.sequence to bigint,
     column project_project_stage.sequence to bigint,
     column project_task.sequence to bigint,
     column project_task_type.sequence to bigint,
     column purchase_order_line.sequence to bigint,
     column quotation_document.sequence to bigint,
     column report_layout.sequence to bigint,
     column res_company.sequence to bigint,
     column res_partner.latest_followup_sequence to bigint,
     column res_partner_bank.sequence to bigint,
     column resource_calendar_attendance.sequence to bigint,
     column restaurant_floor.sequence to bigint,
     column sale_order_line.sequence to bigint,
     column sale_order_option.sequence to bigint,
     column sale_order_template.sequence to bigint,
     column sale_order_template_line.sequence to bigint,
     column slide_answer.sequence to bigint,
     column slide_channel.sequence to bigint,
     column slide_channel_tag.group_sequence to bigint,
     column slide_channel_tag.sequence to bigint,
     column slide_channel_tag_group.sequence to bigint,
     column slide_question.sequence to bigint,
     column slide_slide.sequence to bigint,
     column slide_slide_resource.sequence to bigint,
     column spreadsheet_dashboard.sequence to bigint,
     column spreadsheet_dashboard_group.sequence to bigint,
     column stock_move.sequence to bigint,
     column stock_package_type.sequence to bigint,
     column stock_picking_type.sequence to bigint,
     column stock_putaway_rule.sequence to bigint,
     column stock_route.sequence to bigint,
     column stock_rule.route_sequence to bigint,
     column stock_rule.sequence to bigint,
     column stock_scrap_reason_tag.sequence to bigint,
     column stock_warehouse.sequence to bigint,
     column survey_question.sequence to bigint,
     column survey_question_answer.sequence to bigint,
     column survey_user_input_line.question_sequence to bigint,
     column theme_ir_asset.sequence to bigint,
     column theme_website_menu.sequence to bigint,
     column utm_stage.sequence to bigint,
     column web_tour_tour.sequence to bigint,
     column web_tour_tour_step.sequence to bigint,
     column website.sequence to bigint,
     column website_configurator_feature.sequence to bigint,
     column website_configurator_feature.menu_sequence to bigint,
     column website_menu.sequence to bigint,
     column website_rewrite.sequence to bigint,
     column website_sale_extra_field.sequence to bigint,
     --- END SEQUENCE

     -- DATA BEGIN
     column account_account_tag.color to bigint drop default,
     column account_aged_trial_balance.period_length to bigint drop default,
     column account_analytic_plan.color to bigint drop default,
     column account_asset_asset.method_number to bigint drop default,
     column account_asset_asset.method_period to bigint drop default,
     column account_asset_category.method_number to bigint drop default,
     column account_asset_category.method_period to bigint drop default,          
     column account_autopost_bills_wizard.nb_unmodified_bills to bigint,          
     column account_chart_template.code_digits to bigint drop default,          
     column account_financial_report.level to bigint drop default,
     column account_journal.color to bigint drop default,
     column account_payment_term_line.days to bigint drop default,
     column account_payment_term_line.days_after to bigint drop default,
     column account_payment_term_line.discount_days to bigint drop default,
     column account_payment_term_line.months to bigint drop default,
     column account_payment_term_line.nb_days to bigint,
     column account_payment_term.discount_days to bigint,          
     column account_reconcile_model.past_months_limit to bigint drop default,
     column account_recurring_template.recurring_interval to bigint drop default,          
     column account_report_line.hierarchy_level to bigint drop default,
     column account_report.load_more_limit to bigint drop default,
     column account_report.prefix_groups_threshold to bigint,
     column account_setup_bank_manual_config.num_journals_without_account to bigint drop default,
     column asset_modify.method_number to bigint drop default,
     column asset_modify.method_period to bigint drop default,          
     column base_module_update.added to bigint drop default,
     column base_module_update.updated to bigint drop default,
     column base_partner_merge_automatic_wizard.maximum_group to bigint drop default,
     column base_partner_merge_automatic_wizard.number_group to bigint drop default,
     column calendar_alarm.duration to bigint drop default,
     column calendar_alarm.duration_minutes to bigint drop default,
     column calendar_event_type.color to bigint drop default,          
     column calendar_recurrence.count to bigint drop default,
     column calendar_recurrence.day to bigint drop default,
     column calendar_recurrence.interval to bigint drop default,
     column card_campaign_tag.color to bigint,
     column crm_iap_lead_industry.color to bigint drop default,
     column crm_iap_lead_mining_request.company_size_max to bigint drop default,
     column crm_iap_lead_mining_request.company_size_min to bigint drop default,
     column crm_iap_lead_mining_request.contact_number to bigint drop default,
     column crm_iap_lead_mining_request.lead_number to bigint drop default,
     column crm_iap_lead_role.color to bigint drop default,
     column crm_lead_role.color to bigint drop default,
     column crm_lead.color to bigint drop default,
     column crm_lead.message_bounce to bigint drop default,
     column crm_recurring_plan.number_of_months to bigint drop default,
     column crm_tag.color to bigint drop default,
     column crm_team_member.assignment_max to bigint drop default,
     column crm_team.color to bigint drop default,
     column data_recycle_model.notify_frequency to bigint,
     column data_recycle_model.time_field_delta to bigint,          
     column decimal_precision.digits to bigint drop default,          
     column delivery_carrier.shipping_insurance to bigint,          
     column discuss_channel_member.new_message_separator to bigint,          
     column event_event_ticket.seats_max to bigint,
     column event_event.seats_max to bigint,
     column event_mail.interval_nbr to bigint,
     column event_mail.mail_count_done to bigint,
     column event_tag.color to bigint,
     column event_type_mail.interval_nbr to bigint,
     column event_type_ticket.seats_max to bigint,
     column event_type.seats_max to bigint,
     column fetchmail_server.google_gmail_access_token_expiration to bigint drop default,
     column fetchmail_server.port to bigint drop default,
     column fetchmail_server.priority to bigint drop default,
     column fleet_vehicle_log_services.vehicle_id to "uuid not null" drop typemod using int-to-uuid,
     column fleet_vehicle_model_brand.model_count to bigint drop default,
     column fleet_vehicle_model.doors to bigint drop default,
     column fleet_vehicle_model.horsepower to bigint drop default,
     column fleet_vehicle_model.model_year to bigint drop default,
     column fleet_vehicle_model.power to bigint drop default,
     column fleet_vehicle_model.seats to bigint drop default,
     column fleet_vehicle_model.vehicle_range to bigint,
     column fleet_vehicle_tag.color to bigint drop default,
     column fleet_vehicle.doors to bigint drop default,
     column fleet_vehicle.horsepower to bigint drop default,
     column fleet_vehicle.power to bigint drop default,
     column fleet_vehicle.seats to bigint drop default,
     column fleet_vehicle.vehicle_range to bigint,
     column followup_line.delay to bigint drop default,
     column gamification_badge.rule_max_number to bigint,
     column gamification_challenge.remind_update_delay to bigint,
     column gamification_goal.remind_update_delay to bigint,
     column gamification_karma_rank.karma_min to bigint,
     column gamification_karma_tracking.new_value to bigint,
     column gamification_karma_tracking.old_value to bigint,
     column hr_applicant_category.color to bigint drop default,
     column hr_applicant.color to bigint drop default,
     column hr_candidate.color to bigint,
     column hr_candidate.message_bounce to bigint,
     column hr_department.color to bigint drop default,
     column hr_employee_category.color to bigint drop default,
     column hr_employee_skill_log.level_progress to bigint drop default,
     column hr_employee.children to bigint drop default,
     column hr_employee.color to bigint drop default,
     column hr_employee.distance_home_work to bigint,
     column hr_employee.km_home_work to bigint drop default,
     column hr_job.color to bigint drop default,          
     column hr_job.expected_employees to bigint drop default,
     column hr_job.no_of_employee to bigint drop default,
     column hr_job.no_of_hired_employee to bigint drop default,
     column hr_job.no_of_recruitment to bigint drop default,          
     column hr_leave_accrual_level.accrual_validity_count to bigint,
     column hr_leave_accrual_level.first_day to bigint drop default,
     column hr_leave_accrual_level.first_month_day to bigint drop default,
     column hr_leave_accrual_level.postpone_max_days to bigint drop default,
     column hr_leave_accrual_level.second_day to bigint drop default,
     column hr_leave_accrual_level.second_month_day to bigint drop default,
     column hr_leave_accrual_level.start_count to bigint drop default,
     column hr_leave_accrual_level.yearly_day to bigint drop default,
     column hr_leave_accrual_plan.carryover_day to bigint,
     column hr_leave_mandatory_day.color to bigint,
     column hr_leave_stress_day.color to bigint drop default,
     column hr_leave_type.color to bigint drop default,
     column hr_leave_type.max_allowed_negative to bigint,
     column hr_skill_level.level_progress to bigint drop default,
     column hr_skill_type.color to bigint,
     column im_livechat_channel_rule.auto_popup_timer to bigint,
     column ir_act_server.activity_date_deadline_range to bigint drop default,
     column ir_act_window.limit to bigint drop default,
     column ir_attachment.file_size to bigint drop default,          
     column ir_cron_progress.done to bigint,
     column ir_cron_progress.remaining to bigint,
     column ir_cron_progress.timed_out_counter to bigint,
     column ir_cron.failure_count to bigint,
     column ir_cron.interval_number to bigint drop default,
     column ir_cron.numbercall to bigint drop default,
     column ir_cron.priority to bigint drop default,
     column ir_mail_server.google_gmail_access_token_expiration to bigint drop default,
     column ir_mail_server.smtp_port to bigint drop default,
     column ir_model_fields.size to bigint drop default,
     column ir_model_fields.tracking to bigint drop default,
     column ir_profile.entry_count to bigint drop default,
     column ir_profile.sql_count to bigint drop default,
     column ir_property.value_integer to bigint drop default,
     column ir_sequence_date_range.number_next to bigint drop default,
     column ir_sequence.number_increment to bigint drop default,
     column ir_sequence.number_next to bigint drop default,
     column ir_sequence.padding to bigint drop default,
     column ir_ui_view.priority to bigint drop default,
     column link_tracker.count to bigint,          
     column mail_activity_plan_template.delay_count to bigint,
     column mail_activity_type.delay_count to bigint drop default,
     column mailing_contact.message_bounce to bigint,
     column mailing_mailing.ab_testing_pc to bigint,
     column mailing_mailing.color to bigint,
     column maintenance_equipment_category.color to bigint drop default,
     column maintenance_equipment.color to bigint drop default,
     column maintenance_equipment.expected_mtbf to bigint,
     column maintenance_equipment.maintenance_count to bigint drop default,
     column maintenance_equipment.maintenance_open_count to bigint drop default,
     column maintenance_equipment.period to bigint drop default,
     column maintenance_request.color to bigint drop default,
     column maintenance_request.repeat_interval to bigint,
     column maintenance_team.color to bigint drop default,
     column mrp_batch_produce.lot_qty to bigint,
     column mrp_bom.days_to_prepare_mo to bigint,
     column mrp_bom.produce_delay to bigint,
     column mrp_production_split.counter to bigint drop default,
     column mrp_routing_workcenter.time_mode_batch to bigint drop default,
     column mrp_workcenter_tag.color to bigint drop default,
     column mrp_workcenter.color to bigint drop default,
     column mrp_workorder.duration_percent to bigint drop default,
     column note_note.color to bigint drop default,
     column note_tag.color to bigint drop default,
     column payment_provider.color to bigint drop default,
     column pos_category.color to bigint,
     column pos_config.limited_partners_amount to bigint drop default,
     column pos_config.limited_products_amount to bigint drop default,
     column pos_order.customer_count to bigint,
     column pos_order.nb_print to bigint drop default,          
     column pos_session.login_number to bigint drop default,
     column product_attribute_value.color to bigint drop default,
     column product_label_layout.custom_quantity to bigint drop default,
     column product_supplierinfo.delay to bigint drop default,
     column product_template_attribute_line.value_count to bigint drop default,
     column product_template_attribute_value.color to bigint drop default,
     column product_template.color to bigint drop default,
     column product_template.sale_delay to bigint,
     column product_template.website_size_x to bigint drop default,
     column product_template.website_size_y to bigint drop default,
     column project_project.color to bigint drop default,
     column project_tags.color to bigint drop default,
     column project_task_recurrence.recurrence_left to bigint drop default,
     column project_task_recurrence.repeat_interval to bigint drop default,
     column project_task_recurrence.repeat_number to bigint drop default,
     column project_task.color to bigint drop default,
     column project_task.recurrence_left to bigint drop default,
     column project_task.repeat_interval to bigint drop default,
     column project_task.repeat_number to bigint drop default,
     column project_update.closed_task_count to bigint,
     column project_update.progress to bigint drop default,
     column project_update.task_count to bigint,
     column purchase_order.invoice_count to bigint drop default,
     column repair_tags.color to bigint drop default,
     column report_paperformat.dpi to bigint drop default,
     column report_paperformat.header_spacing to bigint drop default,
     column report_paperformat.page_height to bigint drop default,
     column report_paperformat.page_width to bigint drop default,
     column res_company.annual_inventory_day to bigint drop default,
     column res_company.attendance_kiosk_delay to bigint drop default,
     column res_company.contract_expiration_notice_period to bigint,
     column res_company.fiscalyear_last_day to bigint drop default,
     column res_company.hr_presence_control_email_amount to bigint drop default,
     column res_company.overtime_company_threshold to bigint drop default,
     column res_company.overtime_employee_threshold to bigint drop default,
     column res_company.quotation_validity_days to bigint drop default,
     column res_company.work_permit_expiration_notice_period to bigint,
     column res_config_settings.crm_auto_assignment_interval_number to bigint drop default,          
     column res_config_settings.delay_alert_contract to bigint drop default,
     column res_config_settings.overtime_company_threshold to bigint drop default,
     column res_config_settings.overtime_employee_threshold to bigint drop default,
     column res_config_settings.tenor_gif_limit to bigint,          
     column res_country.phone_code to bigint drop default,
     column res_currency.decimal_places to bigint drop default,
     column res_currency.iso_numeric to bigint,
     column res_groups.color to bigint drop default,
     column res_partner_category.color to bigint drop default,
     column res_partner.color to bigint drop default,
     column res_partner.customer_rank to bigint drop default,
     column res_partner.message_bounce to bigint drop default,          
     column res_partner.supplier_rank to bigint drop default,
     column res_users_settings.voice_active_duration to bigint drop default,
     column res_users.karma to bigint,
     column res_users.target_sales_done to bigint drop default,
     column res_users.target_sales_invoiced to bigint drop default,
     column res_users.target_sales_won to bigint drop default,
     column restaurant_table.seats to bigint,
     column restaurant_table.table_number to bigint,
     column sale_order_template.number_of_days to bigint drop default,          
     column slide_channel_partner.completed_slides_count to bigint,
     column slide_channel_partner.completion to bigint,
     column slide_channel_tag.color to bigint,
     column slide_channel.color to bigint,
     column slide_channel.karma_gen_channel_finish to bigint,
     column slide_channel.karma_gen_channel_rank to bigint,
     column slide_channel.karma_review to bigint,
     column slide_channel.karma_slide_comment to bigint,
     column slide_channel.karma_slide_vote to bigint,
     column slide_channel.nbr_article to bigint,
     column slide_channel.nbr_certification to bigint,
     column slide_channel.nbr_document to bigint,
     column slide_channel.nbr_infographic to bigint,
     column slide_channel.nbr_quiz to bigint,
     column slide_channel.nbr_video to bigint,
     column slide_channel.total_slides to bigint,
     column slide_channel.total_views to bigint,
     column slide_channel.total_votes to bigint,
     column slide_embed.count_views to bigint,
     column slide_slide_partner.quiz_attempts_count to bigint,
     column slide_slide_partner.vote to bigint,
     column slide_slide.dislikes to bigint,
     column slide_slide.likes to bigint,
     column slide_slide.nbr_article to bigint,
     column slide_slide.nbr_certification to bigint,
     column slide_slide.nbr_document to bigint,
     column slide_slide.nbr_infographic to bigint,
     column slide_slide.nbr_quiz to bigint,
     column slide_slide.nbr_video to bigint,
     column slide_slide.public_views to bigint,
     column slide_slide.quiz_first_attempt_reward to bigint,
     column slide_slide.quiz_fourth_attempt_reward to bigint,
     column slide_slide.quiz_second_attempt_reward to bigint,
     column slide_slide.quiz_third_attempt_reward to bigint,
     column slide_slide.slide_views to bigint,
     column slide_slide.total_slides to bigint,
     column slide_slide.total_views to bigint,          
     column stock_assign_serial.next_serial_count to bigint drop default,
     column stock_location.cyclic_inventory_frequency to bigint drop default,
     column stock_location.posx to bigint drop default,
     column stock_location.posy to bigint drop default,
     column stock_location.posz to bigint drop default,
     column stock_move.next_serial_count to bigint drop default,
     column stock_picking_type.color to bigint drop default,
     column stock_picking_type.reservation_days_before to bigint drop default,
     column stock_picking_type.reservation_days_before_priority to bigint drop default,
     column stock_rule.delay to bigint drop default,
     column survey_question.random_questions_count to bigint,
     column survey_question.scale_max to bigint,
     column survey_question.scale_min to bigint,
     column survey_question.time_limit to bigint,
     column survey_question.validation_length_max to bigint,
     column survey_question.validation_length_min to bigint,
     column survey_survey.attempts_limit to bigint,
     column survey_survey.certification_validity_months to bigint,
     column survey_survey.color to bigint,
     column survey_survey.session_speed_rating_time_limit to bigint,
     column survey_user_input_line.value_scale to bigint,
     column theme_ir_ui_view.priority to bigint drop default,
     column utm_campaign.color to bigint drop default,
     column utm_tag.color to bigint drop default,
     column web_editor_converter_test.integer to bigint drop default,
     column website_snippet_filter.limit to bigint drop default,
     column website_visitor.visit_count to bigint drop default,
     column website.karma_profile_min to bigint,
     column website.product_page_grid_columns to bigint drop default,
     column website.shop_ppg to bigint drop default,
     column website.shop_ppr to bigint drop default,

     --- END DATA NEW

     -- column account_account.create_date to timestamptz drop default,
     -- column account_account.create_date to "timestamptz DEFAULT now()" drop default,
     -- column account_account.create_date to "timestamp not null DEFAULT now()" drop default,
     -- column _id to uuid drop typemod using int-to-uuid,
     -- column create_uid to uuid drop typemod using int-to-uuid,
     -- column '*'.write_uid to uuid drop typemod using int-to-uuid,
     -- column '*'.company_id to uuid drop typemod using int-to-uuid,
     -- column '*'.product_id to uuid drop typemod using int-to-uuid,
     -- type integer with extra auto_increment to uuid drop typemod drop default using int-to-uuid,
     -- type int with extra auto_increment to uuid drop typemod drop default using int-to-uuid,
     -- type bigint with extra auto_increment to uuid drop typemod using int-to-uuid,
     -- type serial to uuid drop typemod drop default using int-to-uuid,
     -- type bigserial to uuid drop typemod drop default using int-to-uuid,
     type integer with extra auto_increment to "uuid DEFAULT uuidv7()" drop typemod drop default using int-to-uuid,
     type int with extra auto_increment to "uuid DEFAULT uuidv7()" drop typemod drop default using int-to-uuid,
     type bigint with extra auto_increment to "uuid DEFAULT uuidv7()" drop typemod using int-to-uuid,
     type serial to "uuid DEFAULT uuidv7()" drop typemod drop default using int-to-uuid,
     type bigserial to "uuid DEFAULT uuidv7()" drop typemod drop default using int-to-uuid,
     type bigint when (>= precision 20) to uuid drop typemod using int-to-uuid,
     type integer to uuid drop typemod drop default using int-to-uuid,
     type varchar to text drop typemod using remove-null-characters
     -- type timestamp to timestamptz drop typemod,
     -- type int with extra auto_increment to bigserial,
     -- type bigint when unsigned to uuid drop typemod,
     -- type bigint with extra auto_increment to bigserial,
     -- type "character varying" to text drop typemod,
     -- INCLUDING ONLY TABLE NAMES MATCHING ~/^tbl_/,'customer_temp'
     -- CREATE EXTENSION pg_trgm;

AFTER LOAD DO
  $$
     CREATE UNIQUE INDEX ir_property_unique_index 
          ON public.ir_property USING btree (fields_id, COALESCE(company_id, , '00000000-0000-0000-0000-000000000000'::uuid), 
          COALESCE(res_id, ''::character varying))
  $$,
  $$
     CREATE INDEX mail_tracking_value_mail_message_id_old_value_integer_task_stag 
          ON public.mail_tracking_value USING btree (mail_message_id, old_value_integer) 
          WHERE (field = '00000000-0000-0000-0000-0000000032cb'::uuid)
  $$,  
  $$
     CREATE UNIQUE INDEX ir_filters_name_model_uid_unique_action_index 
          ON public.ir_filters USING btree (model_id, COALESCE(user_id, '00000000-0000-0000-0000-000000000000'::uuid), 
          COALESCE(action_id, '00000000-0000-0000-0000-000000000000'::uuid), lower((name)::text))
  $$     
;

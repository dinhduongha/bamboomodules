@page
@using Microsoft.AspNetCore.Mvc.Localization
@using Volo.Abp.MultiTenancy
@using Bamboo.Admin.Localization
@using Volo.Abp.AspNetCore.Mvc.UI.Bootstrap.TagHelpers.Modal
@model Bamboo.Abp.LoginUi.Web.Pages.Admin.Members.InviteMemberModalModel
@inject IHtmlLocalizer<AdminResource> L
@inject ICurrentTenant CurrentTenant
@{
    Layout = null;
}
<form method="post" action="@Url.Page("./InviteMemberModal")">
<abp-modal data-modal-name="inviteMemberModal">
    <abp-modal-header title="@L["InviteMember"].Value"></abp-modal-header>
    <abp-modal-body>
        <abp-form id="inviteMemberForm">
            @if (!CurrentTenant.IsAvailable)
            {
                <abp-select asp-for="Member.TenantId" 
                            asp-items="Model.Tenants" 
                            label="@L["Tenant"].Value"/>
            }
            @* Đặt autofocus vào trường UserId để tránh lỗi setSelectionRange trên input type="email" *@
            <abp-input asp-for="Member.UserId" autofocus="true" /> 
            <abp-input asp-for="Member.UserNameOrEmail" />
            <abp-select asp-for="Member.RoleId" 
                        asp-items="Model.Roles" 
                        label="@L["Roles"].Value"
                        data-allow-clear="true" data-placeholder="@L["SelectRole"].Value" />

            @* <abp-select asp-for="Member.Roles" asp-items="Model.Roles" label="@L["Roles"].Value" multiple="true"/> *@
        </abp-form>
    </abp-modal-body>
    <abp-modal-footer buttons="@(AbpModalButtons.Cancel|AbpModalButtons.Save)"></abp-modal-footer>
</abp-modal>
</form>
<script src="/Pages/Members/inviteMemberModal.v2.js"></script>